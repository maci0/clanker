# AssemblyScript's dependency tree has no required lifecycle scripts.
# Keep installs inert so a compromised transitive package cannot execute
# code during `npm ci` / `npm install` (verify.sh already passes
# --ignore-scripts; this makes the default project-wide).
ignore-scripts=true
