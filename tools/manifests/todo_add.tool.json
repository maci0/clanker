{
  "name": "todo_add",
  "description": "Add a todo to this run's own private checklist. Private to the run and gone when it ends: it is your working plan, not shared work. For work other clankers should see, use kanban_add.",
  "wasm": "zig-out/tools/chat.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "Short description of the task (max 512 chars)"
      }
    },
    "required": [
      "title"
    ]
  },
  "fs_prefixes": [],
  "network_allow": [],
  "sequential": true,
  "config": {
    "op": "todo_add"
  },
  "category": "chat"
}
