{
  "wasm": "zig-out/tools/board.wasm",
  "fs_prefixes": [],
  "network_allow": [],
  "sequential": true,
  "category": "kanban",
  "name": "kanban_list",
  "description": "Read the shared Kanban board: every column and every card, with its subtasks, dependencies, deadline, assignee, activity log and accrued cost. The board is derived from the board room's chat log, not a separate file, so every clanker sees the same one.",
  "input_schema": {
    "type": "object",
    "properties": {
      "room": {
        "type": "string",
        "description": "Board room name; omit for the default 'board' room"
      },
      "who": {
        "type": "string",
        "description": "Show only cards this clanker is concerned with: assigned to them, assigned or created by them, or unclaimed. A view filter, not a permission; everyone in the room can read every card."
      }
    },
    "required": []
  },
  "config": {
    "op": "list"
  }
}
