{
  "name": "session_export",
  "description": "render one saved session as a self-contained HTML transcript",
  "wasm": "zig-out/tools/session_export.wasm",
  "input_schema": {
    "type": "object",
    "required": [
      "id"
    ],
    "properties": {
      "id": {
        "type": "string"
      },
      "return_html": {
        "type": "boolean"
      },
      "format": {
        "type": "string",
        "enum": ["html", "md"],
        "description": "Render as a self-contained HTML page (default) or as Markdown. Chooses the file extension of state/exports/<id>.<ext> too."
      },
      "forget": {
        "type": "boolean",
        "description": "Host-internal: delete this session's export instead of writing one, called when its transcript is deleted"
      }
    }
  },
  "fs_prefixes": [
    "state/exports/"
  ],
  "internal": true,
  "category": "harness",
  "session": true
}
