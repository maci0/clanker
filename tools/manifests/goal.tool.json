{
  "name": "goal",
  "description": "design and persist a structured goal",
  "llm_description": "Persist a structured goal. Required: \"objective\", \"completion_criterion\". Optional: \"proof\", \"boundaries\", \"stop_rule\".",
  "wasm": "zig-out/tools/goal.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "objective": {
        "type": "string"
      },
      "completion_criterion": {
        "type": "string"
      },
      "proof": {
        "type": "string"
      },
      "boundaries": {
        "type": "string"
      },
      "stop_rule": {
        "type": "string"
      }
    },
    "required": [
      "objective",
      "completion_criterion"
    ]
  },
  "fs_prefixes": [
    "state/goals.json"
  ],
  "category": "agent"
}
