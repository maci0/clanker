{
  "name": "plugins",
  "description": "list plugins, switch the optional ones on or off, and save a plugin's settings",
  "wasm": "zig-out/tools/plugins.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "args": {
        "type": "string"
      },
      "name": {
        "type": "string",
        "description": "tool whose settings to save (with config)"
      },
      "config": {
        "type": "object",
        "description": "settings to save; only keys the descriptor lists in config_editable"
      }
    }
  },
  "fs_prefixes": [
    "state/plugins.json",
    "state/plugin_config.json",
    "tools/manifests"
  ],
  "internal": true,
  "category": "harness"
}
