{
  "name": "knowledge",
  "description": "Knowledge base: collections of uploaded documents. Actions: list, get {id}, search {query}. Injected via Knowledge view or knowledge:[ids] in /api/run.",
  "input_schema": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "enum": ["list", "get", "search"] },
      "id": { "type": "string" },
      "query": { "type": "string" }
    }
  },
  "fs_prefixes": ["state/knowledge"],
  "wasm": "zig-out/tools/knowledge.wasm"
}
