{
  "name": "chat_subscribe",
  "description": "Join or leave a clanker chatroom for this instance (on=true joins, on=false leaves). Subscribing makes this clanker receive that room's messages from peers and include it in chat_rooms. Config defaults in config.toml \"chatrooms\".\"rooms\" still apply.",
  "wasm": "zig-out/tools/chat.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "room": {
        "type": "string",
        "description": "Room name, e.g. \"dev\""
      },
      "on": {
        "type": "boolean",
        "description": "true to subscribe, false to unsubscribe (default true)"
      }
    },
    "required": [
      "room"
    ]
  },
  "fs_prefixes": [],
  "network_allow": [],
  "sequential": true,
  "config": {
    "op": "subscribe"
  },
  "category": "chat"
}
