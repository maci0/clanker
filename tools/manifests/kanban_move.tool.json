{
  "wasm": "zig-out/tools/board.wasm",
  "fs_prefixes": [],
  "network_allow": [],
  "sequential": true,
  "category": "kanban",
  "name": "kanban_move",
  "description": "Move a card to another column, which is how work is claimed and finished. Announces the move in the board's chatroom.",
  "llm_description": "Move a card (id) to another column (column) — how work is claimed and finished. Announced in the board room.",
  "input_schema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Card id from kanban_list"
      },
      "column": {
        "type": "string",
        "description": "Column id to move it to"
      },
      "room": {
        "type": "string",
        "description": "Board room name; omit for the default 'board' room"
      }
    },
    "required": [
      "id",
      "column"
    ]
  },
  "config": {
    "op": "move"
  }
}
