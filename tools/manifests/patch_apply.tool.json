{
  "name": "patch_apply",
  "description": "Applies a batch of exact-match text changes to files under state/staging/ or state/autoresearch/. Internal: used by the self-improve engine and the autoresearch loop to materialize a proposal, never called by the model.",
  "wasm": "zig-out/tools/patch_apply.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "changes": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "file": { "type": "string" },
            "old": { "type": "string" },
            "new": { "type": "string" }
          },
          "required": ["file", "new"]
        }
      }
    },
    "required": ["changes"]
  },
  "fs_prefixes": ["state/staging", "state/autoresearch"],
  "internal": true,
  "category": "harness"
}
