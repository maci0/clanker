{
  "name": "providers",
  "description": "Check which configured LLM providers answer. {\"action\": \"check\"} pings every provider with a one-token request and reports ok/failed/skipped with latency; {\"action\": \"check\", \"provider\": \"kimi-k3\"} checks one; {\"action\": \"list\"} lists them without spending tokens.",
  "wasm": "zig-out/tools/providers.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "check",
          "list"
        ]
      },
      "provider": {
        "type": "string"
      }
    },
    "required": [
      "action"
    ]
  },
  "fs_prefixes": [],
  "llm": true,
  "network_from_config": "providers",
  "category": "peers"
}
