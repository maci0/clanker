{
  "name": "session_search",
  "description": "Search saved conversations for a substring. Input: {\"q\":\"text\"} (at least 3 characters). Returns matching lines. Resume a hit with clanker repl --session <id>.",
  "llm_description": "Search past conversations. Input: {q} min 3 chars. Returns file:line hits under state/sessions. To continue a hit, tell the operator the session id (filename without .json).",
  "wasm": "zig-out/tools/sessions.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "q": {
        "type": "string",
        "description": "case-insensitive substring, min 3 characters"
      }
    },
    "required": [
      "q"
    ]
  },
  "fs_prefixes": [
    "state/sessions/"
  ],
  "confirm": false,
  "category": "harness",
  "session": true
}
