{
  "name": "docker",
  "description": "query the local Docker daemon via its Unix socket",
  "llm_description": "Query the local Docker daemon via its Unix socket with GET. {path}.",
  "wasm": "zig-out/tools/docker.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string"
      }
    },
    "required": [
      "path"
    ]
  },
  "network_allow": [],
  "fs_prefixes": [],
  "category": "infra"
}
