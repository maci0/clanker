{
  "name": "docker",
  "description": "query the local Docker daemon via its Unix socket",
  "wasm": "zig-out/tools/docker.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": { "type": "string" },
      "method": { "type": "string" }
    },
    "required": ["path"]
  },
  "network_allow": [],
  "fs_prefixes": []
}
