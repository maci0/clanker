{
  "name": "autoresearch",
  "description": "list autoresearch runs or tail a run's ledger. Input {\"run\":\"<id>\", \"last\":20}. No id lists runs.",
  "llm_description": "List autoresearch runs or tail a run's ledger. Input {run:'<id>', last:20}; no id lists runs.",
  "wasm": "zig-out/tools/autoresearch.wasm",
  "input_schema": {
    "type": "object",
    "properties": {
      "op": {
        "type": "string"
      },
      "run": {
        "type": "string"
      },
      "last": {
        "type": "integer"
      }
    }
  },
  "fs_prefixes": ["state/autoresearch/"],
  "category": "agent"
}
