{
  "name": "code_search",
  "description": "search open-source code via Sourcegraph (grep.app's API is Vercel-blocked for non-browser clients). Returns repo/path/line snippets. E.g. query=readSliceShort lang=zig to see real-world Zig usage.",
  "llm_description": "Search open-source code via Sourcegraph; returns repo/path/line snippets. {query, lang?, max?}.",
  "wasm": "zig-out/tools/code_search.wasm",
  "input_schema": { "type": "object", "properties": { "query": { "type": "string" }, "lang": { "type": "string" }, "max": { "type": "integer" } }, "required": ["query"] },
  "network_allow": ["sourcegraph.com"],
  "fs_prefixes": []
}
