# Zig build artifacts
.zig-cache/
zig-out/
zig-pkg/

# AssemblyScript toolchain and its build output
tool-src/ts/node_modules/
tool-src/ts/build/

# Runtime state (logs, history, sessions, improvements) — never commit
state/

# User-local configuration (API keys live in env vars anyway)
config.local.json

# Editor / OS junk
.DS_Store
*.swp
*~
