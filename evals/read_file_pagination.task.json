{
  "name": "read_file_pagination",
  "kind": "task",
  "description": "A bounded read_file call on a file larger than the limit must report next_offset so the caller knows there is more data.",
  "prompt": "Use the read_file tool to read src/agent/loop.zig with offset 0 and limit 1024. Does the result JSON contain the field 'next_offset'? Answer with only YES or NO.",
  "requires_tool": "read_file",
  "criteria": [
    {"includes": ["YES"]}
  ]
}
